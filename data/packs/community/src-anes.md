# ANES — American National Election Studies

A pre/post-election respondent-level survey of US political attitudes
and behaviour, open after free instant registration.

```yaml
- id: src-anes
  name: American National Election Studies
  unit: respondent, pre/post-election
  coverage: "US, 1948-2024"
  access: "open after free instant registration"
  gotcha: "requires a login step, not a bare URL fetch; weight variable names change every wave — read the current codebook"
```

Access requires a login step, not a bare URL fetch. Weight variable
names change every wave — read the current codebook rather than
assuming a name.
