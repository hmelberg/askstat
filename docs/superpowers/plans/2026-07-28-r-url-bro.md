# R-URL-broen Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Standard R-datahenting (`read.csv(url)`, `readr::read_csv(url)`, `jsonlite::fromJSON(url)`) virker i appens webR med cache-deling, proxy-fallback m/ auth og publiserings-baking — deretter læres AI-prompten om til R-pandas-først.

**Architecture:** Kjøretidsbro (funksjonspatch inne i webR), ALDRI kildeoversettelse. Callout-mekanikken er fase 0-verifisert (ledger 2026-07-28): `webr::eval_js` virker på PostMessage-kanalen, workerens origin = sidens origin, sync XHR + `Module.FS`-skriv fra workeren er målt OK, og native nettverk virker allerede for CORS-åpne URL-er. Broen gjør: manifest-oppslag (prefetch fra main thread) → native forsøk → worker-XHR m/ proxy-retry og auth → bytes til FS → R leser lokal fil med egne encoding-args (M2: bytes autoritative).

**Tech Stack:** webR 0.6.0 (PINNET — jamovi-ABI), js/read-bridge.js (ReadBridge), R base/utils + unlockBinding-patching, chrome-devtools MCP for browser-verifisering, eval-harnessen i .superpowers/sdd/eval-harness/.

## Global Constraints

- ALDRI push i openstat — commit lokalt; push er Hans' beslutning.
- webR er PINNET til 0.6.0 (`_loadWebRImpl` importerer `https://webr.r-wasm.org/v0.6.0/webr.mjs`) — ikke oppgrader; native-nettverksatferden er versjonsavhengig.
- netlify dev CACHER edge-moduler i deno-prosessens levetid — restart + 400-smoke FØR eval-måling etter TS-endringer. Chrome cacher js/ — hard-reload m/ ignoreCache før browser-verifisering.
- Wrappere skal håndtere BÅDE posisjonelle og navngitte argumenter (M1-lærdommen fra Pyodide-patchen), være idempotente (attr-vakt — preamble kan re-kjøres), og feile HØYLYTT med status + URL (aldri-stille-kontrakten).
- `ost`-direktivene røres IKKE — broen dekker venstre kolonne i grenseregelen (åpne GET-URL-er), direktivene beholder hemmeligheter/POST/SDMX/montering/kanonisk vokabular.
- Promptendringer (Task 5) er GATET på at Task 1–4 er grønne — bro først, promptmal etterpå.
- Suitene kjøres slik: `node --test "tests/js/*.test.js"` (1020/0 ved start), `cd netlify/edge-functions && deno test --allow-read --allow-env --allow-net` (285/0), `python3 -m pytest -q` (1410/0). Ingen nye røde.
- Eksempelblokk-vakten: deno-testen «promptens egne kodeblokk-eksempler parser rent» kjører ALLE fenced blokker gjennom den ekte grammatikken — nye eksempelblokker dekkes automatisk; ikke omgå den.

---

### Task 1: Skann + insertBytes i ReadBridge

**Files:**
- Modify: `js/read-bridge.js` (SCAN_RE ~linje 33; ny `insertBytes` ved `_seedEntries` ~linje 205; eksporter ~linje 221)
- Test: `tests/js/read-bridge.test.js` (finnes — legg til nye tester)

**Interfaces:**
- Produces: `ReadBridge.scanUrls(script)` matcher nå OGSÅ `read.csv(`, `read.csv2(` og `fromJSON(` med `https?://`- eller `/api/hent?`-literal (R-formene; `readr::read_csv` dekkes allerede av `read_csv`). `ReadBridge.insertBytes(url, bytes, contentType)` legger bytes i cachen (til post-run-import i Task 3; `exportTags` ser dem da ved publisering).

- [ ] **Step 1: Skriv feilende tester**

I `tests/js/read-bridge.test.js`, legg til (følg filens eksisterende require/reset-mønster):

```js
test('scanUrls fanger R-formene read.csv/read.csv2/fromJSON', () => {
  ReadBridge._reset();
  const script = [
    'df <- read.csv("https://a.example/x.csv")',
    'df2 <- utils::read.csv2("/api/hent?url=y")',
    'j <- jsonlite::fromJSON("https://api.worldbank.org/v2/x?format=json")',
    'r <- readr::read_csv("https://b.example/z.csv")',
    'lokal <- read.csv("/tmp/lokal.csv")',           // IKKE med (filsti)
    'prosa <- les.csv("https://c.example/nei.csv")', // IKKE med (ukjent navn)
  ].join('\n');
  assert.deepStrictEqual(ReadBridge.scanUrls(script), [
    'https://a.example/x.csv', '/api/hent?url=y',
    'https://api.worldbank.org/v2/x?format=json', 'https://b.example/z.csv',
  ]);
});

test('insertBytes legger i cache og exportTags baker den', () => {
  ReadBridge._reset();
  const bytes = new Uint8Array([104, 101, 105]); // "hei"
  ReadBridge.insertBytes('https://a.example/x.csv', bytes, 'text/csv');
  const c = ReadBridge.getCached('https://a.example/x.csv');
  assert.strictEqual(c.contentType, 'text/csv');
  assert.deepStrictEqual(Array.from(c.bytes), [104, 101, 105]);
  const tags = ReadBridge.exportTags('df <- read.csv("https://a.example/x.csv")');
  assert.strictEqual(tags.length, 1);
  assert.strictEqual(tags[0].url, 'https://a.example/x.csv');
});

test('insertBytes avviser rått søppel stille-fritt', () => {
  ReadBridge._reset();
  assert.throws(() => ReadBridge.insertBytes('https://a.example/x', 'ikke-bytes', ''), /Uint8Array/);
});
```

- [ ] **Step 2: Kjør og se dem feile**

Run: `node --test tests/js/read-bridge.test.js`
Expected: FAIL — scanUrls returnerer tom liste for R-formene; `ReadBridge.insertBytes is not a function`.

- [ ] **Step 3: Implementer**

I `js/read-bridge.js`, erstatt SCAN_RE (behold kommentaren over, utvid den):

```js
  // Rene string-literaler i leserne — python/duckdb-formene (read_csv m.fl.)
  // OG R-formene (read.csv/read.csv2/fromJSON; readr::read_csv dekkes av
  // read_csv). BEVISST enkel: variabler og uttrykk dekkes av runtime-veiene.
  var SCAN_RE = /\b(?:read_(?:csv|json|parquet)|read\.csv2?|fromJSON)\(\s*(['"])((?:https?:\/\/|\/api\/hent\?)[^'"\n]+)\1/g;
```

og ny funksjon ved `_seedEntries`:

```js
  // Post-run-import fra webR-FS (R-URL-broen): runtime-hentede bytes inn i
  // cachen så exportTags baker dem ved publisering (S4b) uten ny henting.
  function insertBytes(url, bytes, contentType) {
    if (!(bytes instanceof Uint8Array)) throw new Error('insertBytes krever Uint8Array');
    cache[url] = { bytes: bytes, contentType: contentType || '' };
  }
```

og legg `insertBytes: insertBytes,` inn i `global.ReadBridge = {…}`-objektet.

- [ ] **Step 4: Kjør testene grønt**

Run: `node --test tests/js/read-bridge.test.js`
Expected: PASS (alle, inkl. de gamle — spesielt at eksisterende scan-tester fortsatt går: `read_csv` uendret).

- [ ] **Step 5: Full node-suite + commit**

Run: `node --test "tests/js/*.test.js"` → 0 fail.

```bash
git add js/read-bridge.js tests/js/read-bridge.test.js
git commit -m "feat(read-bridge): R-skanneformer (read.csv/fromJSON) + insertBytes for post-run-import"
```

---

### Task 2: rPatchSource() — R-kilden til broen

**Files:**
- Modify: `js/read-bridge.js` (ny funksjon `rPatchSource` rett etter `pyPatchSource` ~linje 171; eksport i ReadBridge-objektet)
- Test: `tests/js/read-bridge.test.js`

**Interfaces:**
- Consumes: ingenting nytt fra Task 1 (uavhengig — kilden er en ren streng).
- Produces: `ReadBridge.rPatchSource()` → R-kildekode (streng) som definerer, i globalenv:
  - `.ost_bridge_config(origin, headers_json)` — kalles per kjøring fra JS.
  - `.ost_bridge_seed(url, path)` — kalles per prefetch-treff fra JS.
  - `.ost_bridge_fetched_json()` — returnerer JSON-array `[{"url":…,"path":…,"contentType":…}]` over runtime-hentede filer (post-run-import).
  - Patcher `read.csv`/`read.csv2` i utils (unlockBinding-mønsteret, BÅDE namespace og package-env), og `jsonlite::fromJSON`/`readr::read_csv` via `setHook(packageEvent(…, "onLoad"))` + direkte hvis alt lastet. Idempotent.

- [ ] **Step 1: Skriv feilende tester (kildekontrakt, pyPatchSource-mønsteret)**

```js
test('rPatchSource: bærer kontraktens byggesteiner', () => {
  const src = ReadBridge.rPatchSource();
  for (const needle of [
    '.ost_bridge_config', '.ost_bridge_seed', '.ost_bridge_fetched_json',
    'unlockBinding', 'lockBinding', '.ost_patch_pkg("utils"', 'package:',
    'webr::eval_js', 'x-user-defined',           // binærtrygg sync-XHR (husets trikset)
    '/api/hent?url=',                            // proxy-retry-formen
    'setRequestHeader',                          // S5: auth-headere på proxyen
    'packageEvent("jsonlite"', 'packageEvent("readr"',
    '.ost_wrapped',                              // idempotens-vakt
    'HTTP ',                                     // høylytt feil m/ status
  ]) assert.ok(src.includes(needle), 'mangler: ' + needle);
  // Aldri stille: ingen tom catch rundt selve hentingen
  assert.ok(!/tryCatch\([^)]*error\s*=\s*function\(e\)\s*NULL/.test(src), 'stille sluking');
});

test('rPatchSource: R-kilden parser som gyldig R (strukturell sjekk)', () => {
  const src = ReadBridge.rPatchSource();
  // Balanserte klammer — fanger transkripsjonsfeil uten R-runtime i CI.
  let depth = 0;
  for (const ch of src) { if (ch === '{') depth++; if (ch === '}') depth--; assert.ok(depth >= 0); }
  assert.strictEqual(depth, 0);
});
```

- [ ] **Step 2: Kjør og se dem feile**

Run: `node --test tests/js/read-bridge.test.js`
Expected: FAIL — `rPatchSource is not a function`.

- [ ] **Step 3: Implementer rPatchSource**

Rett etter `pyPatchSource` i `js/read-bridge.js`:

```js
  // R-kilden for webR-patchene (plan 2026-07-28-r-url-bro). Ligger HER så
  // node-testene kan asserte på den (pyPatchSource-presedensen). Kjøres én
  // gang ved webR-boot (evalRVoid) — all idempotens håndteres R-side.
  // Mekanikken er fase 0-verifisert: eval_js kjører worker-lokalt på
  // PostMessage-kanalen, workerens origin = sidens origin, Module.FS er
  // tilgjengelig fra eval_js, og utils krever unlockBinding (assignInNamespace
  // nekter på låst base-binding).
  function rPatchSource() {
    return [
      'if (!exists(".ost_bridge", envir = globalenv(), inherits = FALSE)) {',
      '  .ost_bridge <- new.env(parent = emptyenv())',
      '  .ost_bridge$paths   <- new.env(parent = emptyenv())',
      '  .ost_bridge$fetched <- new.env(parent = emptyenv())',
      '  .ost_bridge$origin  <- ""',
      '  .ost_bridge$headers <- "{}"',
      '  .ost_bridge$n       <- 0L',
      '}',
      '.ost_json_str <- function(s) {',
      '  s <- gsub("\\\\\\\\", "\\\\\\\\\\\\\\\\", s); s <- gsub("\\"", "\\\\\\\\\\"", s)',
      '  paste0("\\"", s, "\\"")',
      '}',
      '.ost_bridge_config <- function(origin, headers_json) {',
      '  .ost_bridge$origin <- origin; .ost_bridge$headers <- headers_json; invisible(NULL)',
      '}',
      '.ost_bridge_seed <- function(url, path) { assign(url, path, envir = .ost_bridge$paths); invisible(NULL) }',
      '.ost_bridge_fetched_json <- function() {',
      '  us <- ls(.ost_bridge$fetched)',
      '  if (!length(us)) return("[]")',
      '  es <- vapply(us, function(u) { e <- get(u, envir = .ost_bridge$fetched)',
      '    paste0("{\\"url\\":", .ost_json_str(u), ",\\"path\\":", .ost_json_str(e$path),',
      '           ",\\"contentType\\":", .ost_json_str(e$ct), "}") }, character(1))',
      '  paste0("[", paste(es, collapse = ","), "]")',
      '}',
      '.ost_is_bridge_url <- function(x) is.character(x) && length(x) == 1L && !is.na(x) &&',
      '  grepl("^(https?://|/api/hent\\\\?)", x)',
      // Henteren: manifest-treff -> lokal sti; ellers worker-XHR (direkte, så
      // proxy-retry ved status 0) -> bytes til FS -> sti. Høylytt ved feil.
      '.ost_fetch <- function(url) {',
      '  hit <- mget(url, envir = .ost_bridge$paths, ifnotfound = list(NULL))[[1]]',
      '  if (!is.null(hit)) return(hit)',
      '  .ost_bridge$n <- .ost_bridge$n + 1L',
      '  path <- paste0("/ost_cache/rt_", .ost_bridge$n, ".bin")',
      '  abs_url <- if (startsWith(url, "/api/hent?")) paste0(.ost_bridge$origin, url) else url',
      '  proxy_url <- paste0(.ost_bridge$origin, "/api/hent?url=", utils::URLencode(url, reserved = TRUE))',
      '  js <- paste0(',
      '    "(function(){",',
      '    "  function go(u, withHeaders){",',
      '    "    var x = new XMLHttpRequest();",',
      '    "    x.open(\\"GET\\", u, false);",',
      '    "    x.overrideMimeType(\\"text/plain; charset=x-user-defined\\");",',
      '    "    if (withHeaders) { var H = ", .ost_bridge$headers, "; for (var k in H) x.setRequestHeader(k, H[k]); }",',
      '    "    try { x.send(null); } catch (e) { return { status: 0 }; }",',
      '    "    return x;",',
      '    "  }",',
      '    "  try {",',
      '    "    var direct = ", if (startsWith(url, "/api/hent?")) "true" else "false", ";",',
      '    "    var x = go(", .ost_json_str(abs_url), ", direct);",',
      '    "    if (x.status === 0 && !direct) x = go(", .ost_json_str(proxy_url), ", true);",',
      '    "    if (x.status === 0 || x.status >= 400) return \\"ERR:HTTP \\" + x.status;",',
      '    "    var t = x.responseText, u8 = new Uint8Array(t.length);",',
      '    "    for (var i = 0; i < t.length; i++) u8[i] = t.charCodeAt(i) & 0xff;",',
      '    "    try { Module.FS.mkdir(\\"/ost_cache\\"); } catch (e) {}",',
      '    "    Module.FS.writeFile(", .ost_json_str(path), ", u8);",',
      '    "    return \\"OK:\\" + (x.getResponseHeader(\\"Content-Type\\") || \\"\\");",',
      '    "  } catch (e) { return \\"ERR:\\" + String(e).slice(0, 200); }",',
      '    "})()")',
      '  res <- as.character(webr::eval_js(js))',
      '  if (!startsWith(res, "OK:")) {',
      '    code <- sub("^ERR:", "", res)',
      '    stop("kunne ikke hente ", url, " (", if (nzchar(code)) code else "ukjent feil",',
      '         "). CORS-stengte kilder krever /api/hent-proxy og n\\u00f8kkel/innlogging i AI-innstillingene.")',
      '  }',
      '  assign(url, list(path = path, ct = sub("^OK:", "", res)), envir = .ost_bridge$fetched)',
      '  path',
      '}',
      // Wrapper: posisjonelt ELLER navngitt fil-argument (M1-pariteten).
      '.ost_wrap_reader <- function(orig, argname) {',
      '  if (isTRUE(attr(orig, ".ost_wrapped"))) return(orig)',
      '  w <- function(...) {',
      '    a <- list(...); nm <- names(a); if (is.null(nm)) nm <- rep("", length(a))',
      '    idx <- match(argname, nm)',
      '    if (is.na(idx) && length(a) && nm[1] == "") idx <- 1L',
      '    if (!is.na(idx) && .ost_is_bridge_url(a[[idx]])) a[[idx]] <- .ost_fetch(a[[idx]])',
      '    do.call(orig, a)',
      '  }',
      '  attr(w, ".ost_wrapped") <- TRUE',
      '  w',
      '}',
      // utils er base: assignInNamespace NEKTER (låst binding) — unlockBinding
      // på BÅDE namespacet og package-env (verifisert i fase 0-spiken).
      '.ost_patch_binding <- function(env, name, argname) {',
      '  if (!exists(name, envir = env, inherits = FALSE)) return(invisible(NULL))',
      '  cur <- get(name, envir = env)',
      '  patched <- .ost_wrap_reader(cur, argname)',
      '  if (identical(patched, cur)) return(invisible(NULL))',
      '  if (bindingIsLocked(name, env)) { unlockBinding(name, env); on.exit(lockBinding(name, env), add = TRUE) }',
      '  assign(name, patched, envir = env)',
      '  invisible(NULL)',
      '}',
      '.ost_patch_pkg <- function(pkg, funs, argname) {',
      '  if (!requireNamespace(pkg, quietly = TRUE)) return(invisible(NULL))',
      '  for (f in funs) {',
      '    .ost_patch_binding(asNamespace(pkg), f, argname)',
      '    pe <- paste0("package:", pkg)',
      '    if (pe %in% search()) .ost_patch_binding(as.environment(pe), f, argname)',
      '  }',
      '  invisible(NULL)',
      '}',
      '.ost_install_bridge <- function() {',
      '  .ost_patch_pkg("utils", c("read.csv", "read.csv2"), "file")',
      '  .ost_patch_pkg("jsonlite", "fromJSON", "txt")',
      '  .ost_patch_pkg("readr", c("read_csv", "read_csv2"), "file")',
      // Sen-lastede pakker (webr::install midt i økten): onLoad-krok.
      '  setHook(packageEvent("jsonlite", "onLoad"), function(...) .ost_patch_pkg("jsonlite", "fromJSON", "txt"))',
      '  setHook(packageEvent("readr", "onLoad"), function(...) .ost_patch_pkg("readr", c("read_csv", "read_csv2"), "file"))',
      '  invisible(NULL)',
      '}',
      '.ost_install_bridge()',
      ''
    ].join('\n');
  }
```

og legg `rPatchSource: rPatchSource,` i eksport-objektet.

MERK til implementeren: strengen over har DOBBEL escaping (R-kode som JS-strengliteraler, og JS-kode som R-strengliteraler inni der igjen). Verifiser med Step 4-testene OG kjør kilden gjennom en ekte R-parser i Step 5 før commit — ikke stol på øyemål.

- [ ] **Step 4: Kjør node-testene grønt**

Run: `node --test tests/js/read-bridge.test.js`
Expected: PASS.

- [ ] **Step 5: Verifiser at kilden parser i EKTE R (Rscript finnes lokalt; ellers webR i Task 3)**

```bash
node -e 'require("./js/directive-parser.js"); require("./js/data-directives.js"); require("./js/read-bridge.js"); process.stdout.write(globalThis.ReadBridge.rPatchSource())' > /tmp/ost_bridge.R
Rscript -e 'invisible(parse("/tmp/ost_bridge.R")); cat("PARSER-OK\n")' 2>/dev/null || echo "Rscript mangler — Task 3 Step 3 verifiserer i webR i stedet"
```
Expected: `PARSER-OK` (eller fallback-meldingen; da er Task 3 Step 3 obligatorisk gate).

- [ ] **Step 6: Commit**

```bash
git add js/read-bridge.js tests/js/read-bridge.test.js
git commit -m "feat(read-bridge): rPatchSource — R-kjøretidsbroen (unlockBinding-patch, worker-XHR m/ proxy+auth, FS-skriv)"
```

---

### Task 3: Wiring i index.html — boot, per-run, post-run

**Files:**
- Modify: `index.html` — (a) `_loadWebRImpl` (~linje 10160, etter library-overriden), (b) `runHybridR` (~linje 9200, start + slutt), (c) sjekk at `js/read-bridge.js` lastes FØR webR kan boote (script-taggen finnes alt for py-modus — verifiser rekkefølgen med grep).

**Interfaces:**
- Consumes: `ReadBridge.rPatchSource()`, `ReadBridge.prefetchScript(script)`, `ReadBridge.scanUrls(script)`, `ReadBridge.getCached(url)`, `ReadBridge.insertBytes(url, bytes, ct)` (Task 1–2); `webR.FS.writeFile/readFile` (samme API som parquet-veien :8618); `DataLoader.proxyHeaders(authToken, anthropicKey)`; `getAnthropicKey()`.
- Produces: R-kjøringer der `read.csv(url)`-klassen virker med cache/proxy/auth; runtime-hentede bytes importert i ReadBridge post-run.

- [ ] **Step 1: Boot-patch i `_loadWebRImpl`**

Rett etter `library`-override-blokken (`await webR.evalRVoid(\`\nlibrary <- function(pkg, ...) …\`)`), legg til:

```js
        // R-URL-broen (plan 2026-07-28-r-url-bro): patch read.csv-klassen én
        // gang ved boot. Kilden bor i js/read-bridge.js (rPatchSource) så
        // node-testene ser den — pyPatchSource-presedensen.
        if (window.ReadBridge && window.ReadBridge.rPatchSource) {
          await webR.evalRVoid(window.ReadBridge.rPatchSource());
        }
```

- [ ] **Step 2: Per-run-seeding i `runHybridR`**

Rett etter `extractRPackages`-blokken i `runHybridR` (etter pakkeinstallasjonen, FØR selve kjøringen), legg til:

```js
      // R-URL-broen: (1) hint-prefetch (literal-skann; bom koster tid, aldri
      // korrekthet), (2) konfig m/ origin + S5-auth-headere per kjøring
      // (nøkkel lagt inn midt i økten plukkes opp), (3) seed cache-treff som
      // FS-filer + manifest (workeren kan ikke nå main-thread-cachen synkront).
      if (window.ReadBridge) {
        try {
          window.ReadBridge.prefetchScript(src);
          // Samme deps-idiom som ReadBridge.forPyodideSync (S5): kroken
          // window.__readBridgeDeps (index.html:~2122) gir { anthropicKey };
          // proxyHeaders(authToken, anthropicKey) er auth-portets form.
          var _rbDeps = (typeof window.__readBridgeDeps === 'function') ? (window.__readBridgeDeps() || {}) : {};
          var _rbHeaders = (window.DataLoader && window.DataLoader.proxyHeaders)
            ? window.DataLoader.proxyHeaders(_rbDeps.authToken, _rbDeps.anthropicKey) : {};
          await webR.evalRVoid('.ost_bridge_config(' + JSON.stringify(location.origin) + ', ' +
            JSON.stringify(JSON.stringify(_rbHeaders)) + ')');
          var _rbUrls = window.ReadBridge.scanUrls(src);
          for (var _ri = 0; _ri < _rbUrls.length; _ri++) {
            var _rc = await window.ReadBridge.ensure(_rbUrls[_ri]);
            if (_rc && _rc.bytes) {
              var _rp = '/ost_cache/pre_' + _ri + '.bin';
              try { await webR.FS.mkdir('/ost_cache'); } catch (e) { /* finnes */ }
              await webR.FS.writeFile(_rp, _rc.bytes);
              await webR.evalRVoid('.ost_bridge_seed(' + JSON.stringify(_rbUrls[_ri]) + ', ' + JSON.stringify(_rp) + ')');
            }
          }
        } catch (e) { console.warn('R-URL-broen (pre-run):', e); }
      }
```

MERK: `_rbDeps.authToken` — sjekk hvordan direktiv-veien i R-modus bygger deps i dag (grep `resolveAndFetchLoads` i runHybridR-regionen) og bruk NØYAKTIG samme felter; smoke-revisjonen S5 definerte formen.

- [ ] **Step 3: Post-run-import i `runHybridR`**

Etter kjøringen (ved `refreshDatasetSidebarFromR()`-kallet — grep etter det i runHybridR/kallstedene og legg dette inntil):

```js
      // R-URL-broen: runtime-hentede filer -> ReadBridge-cachen, så
      // publiserings-bakingen (S4b, exportTags) dekker dem uten ny henting.
      if (window.ReadBridge && webRReady) {
        try {
          var _fj = await webR.evalRString('.ost_bridge_fetched_json()');
          var _fl = JSON.parse(_fj || '[]');
          for (var _fi = 0; _fi < _fl.length; _fi++) {
            var _fb = await webR.FS.readFile(_fl[_fi].path);
            window.ReadBridge.insertBytes(_fl[_fi].url, _fb, _fl[_fi].contentType || '');
          }
        } catch (e) { console.warn('R-URL-broen (post-run):', e); }
      }
```

- [ ] **Step 4: Browser-verifisering av boot + patch (chrome-devtools, INGEN skjermbilder)**

Forutsetning: `netlify dev` på 8888 (RESTART hvis TS er endret). Ny fane `http://localhost:8888/`, hard-reload m/ ignoreCache. Kjør via evaluate_script:

```js
async () => {
  await window.M2PY.loadWebR();
  const w = window.M2PY.getWebR();
  return {
    patched: await w.evalRString('tryCatch(if (isTRUE(attr(utils::read.csv, ".ost_wrapped"))) "PATCHET" else "IKKE", error=function(e) conditionMessage(e))'),
    lokal: await w.evalRString('tryCatch({ webr::eval_js("Module.FS.writeFile(\\"/tmp/t.csv\\", new TextEncoder().encode(\\"a,b\\\\n1,2\\"))"); x <- read.csv("/tmp/t.csv"); paste("OK", nrow(x), ncol(x)) }, error=function(e) paste("ERR", conditionMessage(e)))'),
  };
}
```
Expected: `{patched: "PATCHET", lokal: "OK 1 2"}`.

- [ ] **Step 5: Browser-verifisering av de tre hente-veiene**

Samme fane. NB: proxy-veien krever nøkkel — seed den midlertidig fra .env (KUN localhost, fjern etterpå):

```js
async () => {
  // (1) CORS-åpen URL — native/bro, (2) /api/hent-relativ — mapping + auth,
  // (3) CORS-stengt — proxy-retry. Kjør (3) FØR nøkkel er satt for å se den
  // HØYLYTTE feilen, og ETTER for å se at retry virker.
  const w = window.M2PY.getWebR();
  const out = {};
  out.corsOpen = await w.evalRString('tryCatch({ x <- read.csv("https://raw.githubusercontent.com/datasciencedojo/datasets/master/titanic.csv"); paste("OK", nrow(x)) }, error=function(e) paste("ERR", substr(conditionMessage(e),1,100)))');
  out.utenNokkel = await w.evalRString('tryCatch({ read.csv("https://fred.stlouisfed.org/graph/fredgraph.csv?id=UNRATE"); "UVENTET-OK" }, error=function(e) substr(conditionMessage(e),1,120))');
  return out;
}
```
Expected: `corsOpen: "OK 891"`, `utenNokkel` inneholder «kunne ikke hente» + «HTTP 401» eller «nøkkel» (høylytt, aldri stille). Deretter sett nøkkel (`window.Keys.set('anthropic', '<fra .env>')`), kjør en NY runHybridR-basert kjøring (config-kallet skjer per run) med fredgraph-URL-en og forvent OK; fjern nøkkelen (`window.Keys.remove('anthropic')`) og lukk fanen.

- [ ] **Step 6: Suiter + commit**

Run: `node --test "tests/js/*.test.js"` → 0 fail (index.html røres ikke av node-testene, men kjør likevel).

```bash
git add index.html
git commit -m "feat(r-modus): R-URL-broen koblet — boot-patch, per-run prefetch/seed/config m/ S5-auth, post-run-import til ReadBridge"
```

---

### Task 4: E2E i appens egen kjørevei + q14-formene

**Files:**
- Modify: `.superpowers/sdd/bro-smoke.md` (nytt avsnitt «§7 R-broen» — sjekkliste for Hans)
- Test: browser-verifisering (chrome-devtools) via appens Kjør-knapp-vei, IKKE evalR direkte

**Interfaces:**
- Consumes: hele Task 1–3.

- [ ] **Step 1: Kjør de MÅLTE feilformene fra evalbatchen gjennom appens kjørevei**

I fanen: bytt til R-modus, sett scriptet (via `document.getElementById('scriptInput').value = …` + kjør-knappen eller `M2PY.runHybridR`-veien — bruk appens faktiske dispatcher, grep «case 'r'» rundt run-knappen), med q14-essensen:

```r
# (1) q14-formen: readr-less standard R mot CORS-åpen URL
fin <- read.csv("https://raw.githubusercontent.com/datasciencedojo/datasets/master/titanic.csv")
print(nrow(fin))
# (2) q14-formen: /api/hent-relativ sti (feilet FØR broen: «cannot open file»)
# krever nøkkel i AI-innstillingene — forventet HØYLYTT feilmelding uten
nor <- read.csv("/api/hent?url=https%3A%2F%2Ffred.stlouisfed.org%2Fgraph%2Ffredgraph.csv%3Fid%3DUNRATE")
print(nrow(nor))
# (3) q2/q14-formen: WB-JSON via jsonlite
j <- jsonlite::fromJSON("https://api.worldbank.org/v2/country/NOR/indicator/SP.POP.TOTL?date=2020:2021&format=json")
print(length(j))
```

Expected m/ nøkkel satt: tre print-verdier i output-panelet (891, >0, 2); sidepanelet viser `fin`/`nor` som data.frames (refreshDatasetSidebarFromR — finnes fra før). Uten nøkkel: (2) gir bro-feilmeldingen m/ HTTP-status, IKKE «cannot open file».

- [ ] **Step 2: Verifiser post-run-importen**

```js
() => (window.ReadBridge.getCached('https://api.worldbank.org/v2/country/NOR/indicator/SP.POP.TOTL?date=2020:2021&format=json') || {}).contentType
```
Expected: en JSON-aktig contentType (importert fra R-kjøringens runtime-manifest) — beviser S4b-koblingen.

- [ ] **Step 3: Skriv §7 i bro-smoke.md**

Nytt avsnitt med de tre skriptene fra Step 1 som klikkbar sjekkliste for Hans (pek på Smoke-eksempel-menyen-mønsteret a0284cd hvis et menypunkt legges til — valgfritt), NB-linjer om nøkkelkravet for (2) og netlify-dev/Chrome-cache-fellene.

- [ ] **Step 4: Commit**

```bash
git add .superpowers/sdd/bro-smoke.md
git commit -m "docs(bro-smoke): §7 R-broen — tre målte former som sjekkliste"
```

---

### Task 5: Promptkirurgi — MODE_R, JSON-API-linjen, regel 5-ordlyd (GATET på Task 1–4 grønne)

**Files:**
- Modify: `netlify/edge-functions/_lib/data-svar-prompt.ts` (MODE_R ~linje 260; DELIVERY-eksempelblokka ~linje 118; EVAL-REGEL 5 ~linje 108)
- Test: `netlify/edge-functions/_lib/data-svar-prompt.test.ts` (needles; eksempelblokk-vakten dekker nye blokker automatisk)

**Interfaces:**
- Consumes: broen levert (Task 1–4) — promptmalen lover nå noe appen faktisk kan.
- Produces: MODE_R lærer standard R for åpne GET-tabeller; DELIVERY-eksemplet dekker JSON-API-tilfellet.

- [ ] **Step 1: Utvid needle-testen (feiler først)**

I mode/needle-testene, legg til:

```ts
  const r = buildDataSvarSystem("r", reg);
  for (const n of ["read.csv(", "fromJSON", "IKKE hent på nytt"]) {
    if (!r.includes(n)) throw new Error("MODE_R mangler: " + n);
  }
  const a2 = buildDataSvarSystem("python", reg);
  if (!a2.includes("JSON-API")) throw new Error("DELIVERY mangler JSON-API-linjen");
```

Run: `deno test --allow-net=none data-svar-prompt.test.ts` → FAIL.

- [ ] **Step 2: MODE_R-kirurgi**

Erstatt MODE_R-blokken med (behold METODEVERKTØYKASSE-innholdet uendret):

```ts
const MODE_R = `\
## Modus: R (WebR)

tidyverse (dplyr, ggplot2, tidyr) og base R. Andre pakker:
\`webr::install("pakke")\`. METODEVERKTØYKASSE: god — lm/glm + pakker kan
installeres (fixest/sandwich KAN mangle i webR — sjekk, og fall ærlig tilbake
til lm med faste effekter som dummyer). Figurer med ggplot2.

DATAHENTING I R — standard R rett fram (appen ruter URL-er via broen, samme
kode virker i RStudio):

\`\`\`r
df <- read.csv("https://…/tabell.csv")            # åpen GET-tabell (probe: cors:true)
j  <- jsonlite::fromJSON("https://…?format=json") # JSON-API (GET, åpen)
# ssb = ost.connect("ssb")
# ledighet = ssb.read("05839", years="2000:2009")
\`\`\`

Direktivene (\`# alias = ost.connect/read\`) brukes KUN for høyre kolonne i
grenseregelen (register/nøkkel/POST/SDMX). En \`navn\` fra en direktivlinje er
FERDIG INNLASTET — IKKE hent på nytt med read.csv/readLines/fromJSON mot
samme kilde (målt feilklasse 2026-07-28); bruk variabelen direkte.

## Svarformat
Kort forklaring (1–3 setninger), deretter ÉN kjørbar \`\`\`r-blokk med
eventuelle ost-direktiver øverst (# eller -- som kommentartegn). Ikke JSON.`;
```

MERK: eksempelblokk-vakten kjører denne nye \`\`\`r-blokken gjennom den ekte grammatikken — direktivlinjene i den MÅ parse rent (ingen halekommentar på direktivlinjer!).

- [ ] **Step 3: JSON-API-prosalinje i DELIVERY + regel 5-ordlyd**

IKKE legg JSON-API-tilfellet som kodelinje i eksempelblokka (pd.read_json
mot WB-JSON gir nestet struktur — en halvsann kodelinje lærer bort feil form).
Legg i stedet en PROSA-linje rett under eksempelblokka i DELIVERY:

```
JSON-API-er (ikke tabellform, f.eks. World Bank ?format=json): bruk
registerets adapter (\`# wb = ost.connect("worldbank")\`) eller les JSON-en
DIREKTE (\`jsonlite::fromJSON\` i R; i Python: parse \`json.loads\` av en
probe-verifisert cors:true-GET via broens \`pd.read_json\` når formen er flat)
— ALDRI urllib/requests-kode (målt feilklasse 2026-07-28, «JSON-API-hullet»).
```

Og i EVAL-REGEL 5, erstatt «den er nøkkelfri og CORS-åpen» med «den er
nøkkelfri (CORS varierer — stol på PROBEN, målt cors:false 2026-07-28;
proxy da)».

- [ ] **Step 4: Kjør deno-suitene grønt**

Run: `cd netlify/edge-functions && deno test --allow-read --allow-env --allow-net`
Expected: 0 fail — inkl. eksempelblokk-vakten på den nye r-blokken.

- [ ] **Step 5: Commit**

```bash
git add netlify/edge-functions/_lib/data-svar-prompt.ts netlify/edge-functions/_lib/data-svar-prompt.test.ts
git commit -m "feat(data-svar): MODE_R lærer standard R m/ bro; JSON-API-linje i DELIVERY; regel 5 probe-sannhet"
```

---

### Task 6: Måling — re-kjør q3/q7/q14 + q2

**Files:**
- Modify: `docs/eval/data-svar-evalsett.md` (resultatlogg), `.superpowers/sdd/progress.md` (ledger)
- Verktøy: `.superpowers/sdd/eval-harness/` (run.mjs/analyze.mjs), netlify dev på 8888, nøkkel i .env

- [ ] **Step 1: RESTART netlify dev** (edge-modul-cachen!) + 400-smoke mot /api/data-svar.

- [ ] **Step 2: Kjør batchen (Deep)**

```bash
H=/Users/hom/Documents/GitHub/openstat/.superpowers/sdd/eval-harness
EVAL=<øktens scratchpad>/eval && mkdir -p "$EVAL"   # run.mjs <spm> <modus> <utdir> <qid> <depth>
cd "$H"
node run.mjs "Hvordan har boligprisene i Norge utviklet seg sammenlignet med lønningene?" r "$EVAL" q03 deep
node run.mjs "Hvor mye har vaksinasjonsdekningen for meslinger endret seg globalt?" r "$EVAL" q07 deep
node run.mjs "Sammenlign befolkningsveksten i Finland og Norge siden 2000." r "$EVAL" q14 deep
node run.mjs "Er det en sammenheng mellom BNP per innbygger og CO₂-utslipp per land?" python "$EVAL" q02 deep
```

+ `analyze.mjs` per svar (adHocFetch-regexen dekker readLines/download.file fra 2026-07-28).

- [ ] **Step 3: Døm mot kriteriene**

Suksess = re-fetch-klassen borte i r-svarene (direktivvariabler brukt ELLER standard-R-former broen fanger; ingen readLines/httr-ad-hoc), q2 uten urllib (adapter eller ren read_json/fromJSON), ingen NYE stille feil. VIKTIG: et r-script som bruker `read.csv(åpen URL)` direkte er nå PASS på kriterium 3 (grenseregelen v2), ikke FAIL — døm mot Task 5-prompten, og noter kriterie-teksten i evalsettet hvis den må presiseres.

- [ ] **Step 4: Logg + ledger + commit**

Resultatrader i evalsett-loggen + rundeoppsummering i progress.md (mønster: «=== R-URL-BRO-RUNDEN … ===»).

```bash
git add docs/eval/data-svar-evalsett.md
git commit -m "eval(data-svar): R-bro-batchen målt — q3/q7/q14/q2"
```

IKKE push — Hans' beslutning.
