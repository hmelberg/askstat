# label: «# meta» + SSB — metadata hentes fra kilden
# Eksempel: pxweb-kilder får metadata rett fra kilden. Trykk ⓘ ved «bef»
# i Datasett-listen: notatet under vises øverst, sammen med proveniens,
# rader × kolonner og — siden bef kommer fra en registerkilde — kildens
# egen tittel/utgiver/tillit (fra /api/metadata, lazy ved klikk).
# Kilderaden «ssb» under «Tilkoblede kilder» åpnes med ett klikk og viser
# HELE variabellisten fra kilden (inkl. Alder, som SSB fjerner fra bef
# fordi den ikke velges eksplisitt) inni containeren — klikk Alder der
# for kodelisten. 💬 åpner kommentartråden direkte i sidepanelet.
# ssb = ost.connect("https://data.ssb.no/api/pxwebapi/v2/tables", kind="pxweb")
# bef = ssb.read("05839", years="2000:2009", indicators=["Personer"])
# meta.bef.note = "Folkemengde etter alder og kjønn 2000-2009 (SSB tabell 05839)"
# meta.bef.link = {"https://www.ssb.no/befolkning": "Om SSBs befolkningsstatistikk"}
import pandas_brython as pd

show(bef.head(10))
