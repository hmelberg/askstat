# label: «# meta» + SSB — metadata hentes fra kilden
# Eksempel: pxweb-kilder får metadata rett fra kilden. ⓘ på datasettraden «bef»
# i Datasett-lista viser notatet under øverst, proveniens, rader × kolonner
# og kildens egen tittel/utgiver/tillit fra /api/metadata (lazy, først ved
# klikk). Kilderaden «ssb» under «Tilkoblede kilder» åpnes med ett klikk og
# viser hele variabellisten fra kilden (inkl. Alder, som SSB fjerner fra
# bef siden den ikke velges eksplisitt) — klikk Alder der for kodelisten.
# 💬 åpner kommentartråden direkte i sidepanelet.
# ssb = ost.connect("https://data.ssb.no/api/pxwebapi/v2/tables", kind="pxweb")
# bef = ssb.read("05839", years="2000:2009", indicators=["Personer"])
# meta.bef.note = "Folkemengde etter alder og kjønn 2000-2009 (SSB tabell 05839)"
# meta.bef.link = {"https://www.ssb.no/befolkning": "Om SSBs befolkningsstatistikk"}
import pandas as pd

bef.head(10)
