# label: wbgapi — World Bank via micropip
# group: Libraries
# Spike-script (docs/2026-08-spike-wbgapi-sdmx1.md): kan det EKTE World
# Bank-biblioteket installeres og hente data i Pyodide? Det er nøyaktig det
# treningsdataene til modellene inneholder — virker dette rent, er det et
# argument for å la python-modus bruke bibliotekene direkte (ROADMAP «DSL
# vs. LLM-vaner» pkt 2). Noter utfallet i memoet.
import micropip
await micropip.install("wbgapi")
import wbgapi as wb
df = wb.data.DataFrame("SH.XPD.CHEX.GD.ZS", ["NOR", "SWE"], time=range(2015, 2023))
print(df.head())
print("WBGAPI OK, form:", df.shape)
