# label: sdmx1 — OECD dataflows via micropip
# group: Libraries
# Spike-script (docs/2026-08-spike-wbgapi-sdmx1.md): kan sdmx1 (arvtakeren
# etter pandasdmx) installeres og snakke med OECD i Pyodide? Virker det,
# kan det på sikt erstatte håndrullet SDMX-nøkkelbygging (sdmxKeyDims/
# sdmxKeyPath i js/api-kinds.js). Noter utfallet i memoet — install-feil
# (f.eks. lxml/msgspec-avhengigheter) er OGSÅ et svar.
import micropip
await micropip.install("sdmx1")
import sdmx
oecd = sdmx.Client("OECD")
fl = oecd.dataflow()
print("SDMX1 OK, antall dataflows:", len(fl.dataflow))
