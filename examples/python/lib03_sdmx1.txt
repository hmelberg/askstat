# label: sdmx1 — OECD dataflows via auto-install
# group: Libraries
# Spike-script (docs/2026-08-spike-wbgapi-sdmx1.md): kan sdmx1 (arvtakeren
# etter pandasdmx) snakke med OECD i Pyodide? Modulen heter `sdmx`, PyPI-
# pakken `sdmx1` — appens auto-install har en aliastabell for akkurat slike
# navnesprik. Virker dette, kan det på sikt erstatte håndrullet SDMX-
# nøkkelbygging (sdmxKeyDims/sdmxKeyPath i js/api-kinds.js). Install-feil
# (f.eks. lxml/msgspec-avhengigheter) er OGSÅ et svar — noter i memoet.
import sdmx
oecd = sdmx.Client("OECD")
fl = oecd.dataflow()
print("SDMX1 OK, antall dataflows:", len(fl.dataflow))
