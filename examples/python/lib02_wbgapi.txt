# label: wbgapi — World Bank via auto-install
# group: Libraries
# Spike-script (docs/2026-08-spike-wbgapi-sdmx1.md): kan det EKTE World
# Bank-biblioteket brukes i Pyodide? Appen installerer manglende imports
# automatisk (micropip bak kulissene) — derfor INGEN micropip-linje her;
# toppnivå-await støttes ikke i kjøringen. Noter utfallet i memoet.
import wbgapi as wb
df = wb.data.DataFrame("SH.XPD.CHEX.GD.ZS", ["NOR", "SWE"], time=range(2015, 2023))
print(df.head())
print("WBGAPI OK, form:", df.shape)
