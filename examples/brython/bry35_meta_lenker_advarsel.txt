# label: «# meta» — flere lenker, lister og advarsel
# Eksempel: ÉN linje per nøkkel — «=» overskriver, den akkumulerer ikke.
# Flere verdier samles i verdien selv: en LISTE gir flere notater, en DICT
# {url: etikett} gir flere lenker med etikett (en ren streng eller liste av
# strenger gir lenker uten etikett). Trykk ⓘ ved «iris» i Datasett-listen
# for hele notatsamlingen; kilderaden «iris» under «Tilkoblede kilder»
# åpnes med ett klikk og viser variabellisten inni containeren, og 💬 der
# åpner kommentartråden direkte i sidepanelet. Mål som ikke finnes
# (skrivefeil) gir en ⚠-advarsel nederst i «Tilkoblede kilder» — aldri
# stille dropp.
# iris = ost.connect("https://raw.githubusercontent.com/hmelberg/openstat/main/data/iris.csv")
# iris = ost.read("https://raw.githubusercontent.com/hmelberg/openstat/main/data/iris.csv")
# meta.iris.note = ["Fishers irisdata (1936): 150 blomster, tre arter", "Målt i cm på beger- og kronblad"]
# meta.iris.link = {"https://en.wikipedia.org/wiki/Iris_flower_data_set": "Wikipedia", "https://archive.ics.uci.edu/dataset/53/iris": "UCI-arkivet"}
# meta.iris.sepal_length.note = "Begerbladlengde i cm"
# meta.irsi.note = "Denne skrivefeilen gir en ⚠-advarsel i sidepanelet"
import pandas_brython as pd

show(iris.groupby("species").mean())
