#py
# Encrypted source on the web (safepy-enc-v1) — three ways:
#
# 1) Key in the script (simple, least secure):
# df = ost.read("https://raw.githubusercontent.com/<user>/<repo>/main/helse.enc.json", secret_key="PASTE-KEY-HERE")
#
# 2) Key via prompt (the key never ends up in the script):
# h = ost.connect("https://raw.githubusercontent.com/<user>/<repo>/main/helse.enc.json", secret_key="ask")
# df = h.read()
#
# 3) Registered source (the owner has shared access with you — log in first):
# h = ost.connect("helse2025")
# df = h.read()

df.head()
